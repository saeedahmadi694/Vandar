name: Release

on:
  push:
    branches:
      - release

jobs:
  release_vandar_core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Build and Publish VandarSharp.Core
        working-directory: VandarSharp/VandarSharp.Core
        run: |
          dotnet clean
          dotnet restore
          dotnet build
          dotnet pack --configuration Release
          ls ./bin/Release/
          dotnet nuget push ./bin/Release/VandarSharp.Core.*.nupkg --source ${{ secrets.SERVER_NUGET }} --api-key ${{ secrets.NUGET_KEY }}

  release_vandar_aspnetcore:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Build and Publish VandarSharp.AspNetCore
        working-directory: VandarSharp/VandarSharp.AspNetCore
        run: |
          dotnet clean
          dotnet restore
          dotnet build
          dotnet pack --configuration Release
          ls ./bin/Release/
          dotnet nuget push ./bin/Release/VandarSharp.AspNetCore.*.nupkg --source ${{ secrets.SERVER_NUGET }} --api-key ${{ secrets.NUGET_KEY }}
